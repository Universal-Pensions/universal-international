{
  "//": "A25-007 — e2e/ TypeScript (46 specs/fixtures/helpers + ../playwright.config.ts) was never type-checked by any script (only build:api's tsc, scoped to server/tsconfig.json, ran; its include list never mentioned e2e/). This project exists ONLY for `npm run typecheck` (see package.json) — Playwright itself transpiles these files with esbuild at run time and does not read this file. moduleResolution is 'bundler' (not server/tsconfig.json's 'NodeNext') because every relative import here is extension-less, matching how Playwright's own esbuild transform actually resolves them — NodeNext would demand explicit '.js' extensions on ~90 import sites for no real type-safety gain.",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "bundler",
    "target": "ES2022",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "allowJs": true,
    "types": ["node"]
  },
  "include": ["**/*.ts", "../playwright.config.ts"],
  "exclude": ["../node_modules/**"]
}
